---
status: APPROVED
approved_by: user
version: v1
sprint: 1
phase: architecture
sprint_id: sprint-v1
created: 2026-06-08
updated: 2026-06-08 22:21
---

# Event Contracts — 

<!-- Living Truth root for event contracts. Each event is a mergeable anchored block. -->

<!-- ## Stable ID Anchor Convention (Phase 9+)
     Each EVT-NNN block in §2 MUST be preceded by `&lt;!-- ID: EVT-NNN --&gt;` on its own line
     above the `### EVT-NNN: {EventName}` heading.
     Atomic ID (all modes — Guided AND Freedom): `python .prism/core/tools/get_next_id.py --type EVT`
     Strict format: `EVT-\d{3,}` (zero-padded ≥3 digits). -->

<!-- PRISM:INDEX:START (auto-generated by seal_sprint.py — do not edit by hand) -->

## Index

| ID | Title |
|---|---|
| EVT-001 | SeatHeld |
| EVT-002 | PaymentConfirmed |
| EVT-003 | TicketScanned |
| EVT-004 | TicketExchanged |

<!-- PRISM:INDEX:END -->

<!-- ID: EVT-001 -->
### EVT-001: SeatHeld

- **Purpose**: In-process domain event emitted after seats are held for an order.
- **Publisher**: Order & Payment Module.
- **Consumers**: Audit Module; optional future notification hook.
- **Delivery guarantee**: in-process within transaction for v1.
- **Ordering requirement**: Yes, per order.
- **Idempotency expectation**: consumer treats `orderId` as business idempotency key.

#### Payload Schema

| Field | Type | Required | Description | Example |
|---|---|---|---|---|
| `eventId` | UUID | Yes | unique event ID | `"evt-uuid"` |
| `occurredAt` | ISO UTC datetime | Yes | business occurrence time | `"2026-06-08T05:12:00Z"` |
| `traceId` | string | Yes | trace correlation | `"trace-123"` |
| `requestId` | string | Yes | originating request ID | `"req-123"` |
| `orderId` | UUID | Yes | held order | `"order-uuid"` |
| `userId` | UUID | Yes | buyer | `"user-uuid"` |
| `seatIds[]` | UUID[] | Yes | held seats | `["seat-1"]` |
| `expiresAt` | ISO UTC datetime | Yes | hold expiry | `"2026-06-08T05:22:00Z"` |

#### Messaging Contract

Kafka Contract: N/A — no Kafka/event bus in v1 demo. Event is an in-process application event.

#### Failure Handling

| Field | Value |
|---|---|
| Publish failure persistence | Same transaction; no separate broker persistence |
| Retry policy | N/A — transaction rollback on critical consumer failure |
| Replay required | No for v1 |
| Replay window / TTL | N/A |
| Max delivery / replay attempts | 1 in-process attempt |
| DLQ behavior | N/A — no broker; failed audit write rolls back the seat hold |

<!-- ID: EVT-002 -->
### EVT-002: PaymentConfirmed

- **Purpose**: In-process domain event emitted when admin confirms payment.
- **Publisher**: Order & Payment Module.
- **Consumers**: Ticket & Scan Module, Audit Module.
- **Delivery guarantee**: in-process within transaction.
- **Ordering requirement**: Yes, per order.
- **Idempotency expectation**: order state guard prevents duplicate issuance.

#### Payload Schema

| Field | Type | Required | Description | Example |
|---|---|---|---|---|
| `eventId` | UUID | Yes | unique event ID | `"evt-uuid"` |
| `occurredAt` | ISO UTC datetime | Yes | confirmation time | `"2026-06-08T05:12:00Z"` |
| `traceId` | string | Yes | trace correlation | `"trace-123"` |
| `requestId` | string | Yes | originating request ID | `"req-123"` |
| `orderId` | UUID | Yes | confirmed order | `"order-uuid"` |
| `adminUserId` | UUID | Yes | confirming admin | `"admin-uuid"` |
| `confirmedAt` | ISO UTC datetime | Yes | admin decision time | `"2026-06-08T05:12:00Z"` |

#### Messaging Contract

Kafka Contract: N/A — no Kafka/event bus in v1 demo. Event is an in-process application event.

#### Failure Handling

| Field | Value |
|---|---|
| Publish failure persistence | Same transaction |
| Retry policy | N/A — failed ticket issuance rolls back confirmation |
| Replay required | No for v1 |
| Replay window / TTL | N/A |
| Max delivery / replay attempts | 1 in-process attempt |
| DLQ behavior | N/A — no broker; failed ticket/audit write rolls back confirmation |

<!-- ID: EVT-003 -->
### EVT-003: TicketScanned

- **Purpose**: In-process domain event emitted after a valid ticket scan.
- **Publisher**: Ticket & Scan Module.
- **Consumers**: Audit Module.
- **Delivery guarantee**: in-process within transaction.
- **Ordering requirement**: Yes, per ticket.
- **Idempotency expectation**: `ISSUED -> USED_SCANNED` atomic transition prevents duplicate scan.

#### Payload Schema

| Field | Type | Required | Description | Example |
|---|---|---|---|---|
| `eventId` | UUID | Yes | unique event ID | `"evt-uuid"` |
| `occurredAt` | ISO UTC datetime | Yes | scan time | `"2026-06-08T05:12:00Z"` |
| `traceId` | string | Yes | trace correlation | `"trace-123"` |
| `requestId` | string | Yes | originating request ID | `"req-123"` |
| `ticketId` | UUID | Yes | scanned ticket | `"ticket-uuid"` |
| `scanSource` | string | No | scanner source label | `"gate-a"` |

#### Messaging Contract

Kafka Contract: N/A — no Kafka/event bus in v1 demo. Event is an in-process application event.

#### Failure Handling

| Field | Value |
|---|---|
| Publish failure persistence | Same transaction |
| Retry policy | N/A — scan state rolls back on critical audit failure |
| Replay required | No for v1 |
| Replay window / TTL | N/A |
| Max delivery / replay attempts | 1 in-process attempt |
| DLQ behavior | N/A — no broker |

<!-- ID: EVT-004 -->
### EVT-004: TicketExchanged

- **Purpose**: In-process domain event emitted after exchange confirmation.
- **Publisher**: Ticket & Scan Module.
- **Consumers**: Audit Module.
- **Delivery guarantee**: in-process within transaction.
- **Ordering requirement**: Yes, per original ticket.
- **Idempotency expectation**: exchange order state guard.

#### Payload Schema

| Field | Type | Required | Description | Example |
|---|---|---|---|---|
| `eventId` | UUID | Yes | unique event ID | `"evt-uuid"` |
| `occurredAt` | ISO UTC datetime | Yes | exchange time | `"2026-06-08T05:12:00Z"` |
| `traceId` | string | Yes | trace correlation | `"trace-123"` |
| `requestId` | string | Yes | originating request ID | `"req-123"` |
| `oldTicketId` | UUID | Yes | ticket marked exchanged | `"ticket-old"` |
| `newTicketId` | UUID | Yes | newly issued ticket | `"ticket-new"` |
| `orderId` | UUID | Yes | exchange order | `"order-uuid"` |

#### Messaging Contract

Kafka Contract: N/A — no Kafka/event bus in v1 demo. Event is an in-process application event.

#### Failure Handling

| Field | Value |
|---|---|
| Publish failure persistence | Same transaction |
| Retry policy | N/A — exchange transaction rolls back on critical failure |
| Replay required | No for v1 |
| Replay window / TTL | N/A |
| Max delivery / replay attempts | 1 in-process attempt |
| DLQ behavior | N/A — no broker |
