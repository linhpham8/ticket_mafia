# QA Skill Suite Agent Instructions

`SKILL.md` is the only routing source of truth.

For any QA/QC/testing request:

1. Read `SKILL.md`.
2. Select exactly one routed sub-skill unless the user asks for orchestration.
3. Read only that sub-skill and the references it explicitly asks for.
4. Use `references/INDEX.md` to choose optional references; do not open all references.
5. Apply `governance/GOVERNANCE.md` before finalizing, publishing, or updating external systems.

Do not duplicate or maintain a separate routing table in this file.
